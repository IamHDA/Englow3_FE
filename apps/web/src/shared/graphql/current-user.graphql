query CurrentUser {
  me {
    id
    email
    fullName
    displayName
    gender
    birthDate
    avatarUrl
    bannerUrl
    onboardingStep
    onboardingState {
      certificateLearner
      currentLevel
      targetCertificateType
      targetScore
      targetDate
      targetSkills
    }
  }
}
