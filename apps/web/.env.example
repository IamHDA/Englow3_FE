# Copy sang .env.local rồi điền giá trị của project Supabase.
# Cả hai biến đều lộ ra client (tiền tố NEXT_PUBLIC_) — chỉ dùng publishable/anon key,
# tuyệt đối không đặt service_role key ở đây.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
